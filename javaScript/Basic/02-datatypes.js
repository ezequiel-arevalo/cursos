// Cadenas de Texto (Strings)
let name = "Ezequiel Arevalo";
let alias = "Ezequiel-Arevalo";
let email = "ezequiel-arevalo@outlook.com";

// Números (number)
let age = 37;
let height = 1.77;

// Booleanos (Boolean)
let isAuth = true;
let isLogout = false;

// Undefined
// La variable fue inicializada pero no declarada, no tiene un valor asignada, su valor puede ser any (cualquiera)
let persona;
console.log(persona);

// Null
// La variable la inicializó y le declaro un valor nulo, como indicando que el valor no fue seleccionado, supongamos un dropdown por defecto el valor será null a menos que el usuario seleccione algun valor
let contador = null;

// Symbol
// Son utiles para crear algo unico e inmutable
let imposible = Symbol('mySymbol');

// Infinity
let finito = Infinity;

// BigInt
// Número entero grande
let gordo = BigInt(123456789012345678901234567890123456789012345678901234567890);